# --- INFRASTRUCTURE ---
# DB 0 : Dédiée à Next.js (ISR & Cache UI)
# En local avec docker-compose : valkey://localhost:6379/0
VALKEY_URL=valkey://localhost:6379/0

# --- API & BACKEND ---
# URL de l'API Go (utilisée par le serveur pour le fetching)
API_URL=http://localhost:8081

# URL publique de l'API Go (utilisée par le navigateur)
NEXT_PUBLIC_API_URL=http://localhost:8081

# --- SECURITY ---
# Secret pour l'endpoint /api/revalidate (doit correspondre au Backend)
REVALIDATE_SECRET=dev_secret_change_me_in_prod

# Secret pour les sessions (Jose / JWT)
SESSION_SECRET=votre_session_secret_tres_long_et_aleatoire

# --- CONFIG ---
NEXT_TELEMETRY_DISABLED=1
