# --- INFRASTRUCTURE ---
# DB 0 : Dédiée à Next.js (ISR & Cache UI)
VALKEY_URL=valkey://localhost:6379/0

# DB 1 : Dédiée à l'API Go (Sessions, Rate Limiting, Business Logic)
BACKEND_VALKEY_URL=valkey://localhost:6379/1

# --- API & BACKEND ---
# URL interne de l'API Go (utilisée côté serveur)
API_URL=http://api.cedra.internal

# URL publique de l'API Go (utilisée côté client)
NEXT_PUBLIC_API_URL=https://api.cedra.com

# --- SECURITY ---
# Secret pour l'endpoint /api/revalidate (doit correspondre à celui de l'API Go)
REVALIDATE_SECRET=votre_secret_fort_ici

# Secret pour les sessions NextAuth / Jose
SESSION_SECRET=generer_un_secret_aleatoire_ici

# --- NEXT.JS CONFIG ---
# Désactiver la télémétrie en prod
NEXT_TELEMETRY_DISABLED=1
NODE_ENV=production
